---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: podautoscalers.autoscaling.aibrix.ai
spec:
  group: autoscaling.aibrix.ai
  names:
    kind: PodAutoscaler
    listKind: PodAutoscalerList
    plural: podautoscalers
    singular: podautoscaler
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.minReplicas
      name: MINPODS
      type: integer
    - jsonPath: .spec.maxReplicas
      name: MAXPODS
      type: integer
    - jsonPath: .status.actualScale
      name: REPLICAS
      type: integer
    - jsonPath: .spec.scalingStrategy
      name: STRATEGY
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              maxReplicas:
                format: int32
                type: integer
              metricsSources:
                items:
                  properties:
                    endpoint:
                      type: string
                    metricSourceType:
                      enum:
                      - pod
                      - resource
                      - custom
                      - external
                      - domain
                      type: string
                    path:
                      type: string
                    port:
                      type: string
                    protocolType:
                      enum:
                      - http
                      - https
                      type: string
                    targetMetric:
                      type: string
                    targetValue:
                      type: string
                  required:
                  - metricSourceType
                  - targetMetric
                  - targetValue
                  type: object
                minItems: 1
                type: array
              minReplicas:
                format: int32
                type: integer
              scaleTargetRef:
                properties:
                  apiVersion:
                    type: string
                  fieldPath:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                  resourceVersion:
                    type: string
                  uid:
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              scalingStrategy:
                enum:
                - HPA
                - KPA
                - APA
                - CPA
                type: string
              costOptimization:
                description: |-
                  CostOptimization controls cost-aware behavior for the CPA strategy.
                  Ignored for HPA/KPA/APA. When ScalingStrategy is CPA, the controller
                  picks the cheapest (replica count x GPU type) combination that satisfies
                  the target metric value.
                properties:
                  mode:
                    description: Mode determines how the CPA algorithm trades cost against performance.
                    enum:
                    - min-cost
                    - balanced
                    - min-latency
                    - spot-first
                    - reserved-first
                    type: string
                  maxCostPerHour:
                    description: MaxCostPerHour is a hard budget cap in USD/hour.
                    format: double
                    type: number
                  candidateGpuTypes:
                    description: CandidateGPUTypes is the set of GPU types CPA is allowed to choose from.
                    items:
                      type: string
                    type: array
                  preferredCostClass:
                    description: PreferredCostClass biases the algorithm toward a particular pricing class.
                    enum:
                    - spot
                    - reserved
                    - on-demand
                    - any
                    type: string
                type: object
              subTargetSelector:
                properties:
                  roleName:
                    type: string
                type: object
            required:
            - maxReplicas
            - scaleTargetRef
            - scalingStrategy
            type: object
          status:
            properties:
              actualScale:
                format: int32
                type: integer
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              desiredScale:
                format: int32
                type: integer
              lastScaleTime:
                format: date-time
                type: string
              scalingHistory:
                items:
                  properties:
                    error:
                      type: string
                    newScale:
                      format: int32
                      type: integer
                    previousScale:
                      format: int32
                      type: integer
                    reason:
                      type: string
                    success:
                      type: boolean
                    timestamp:
                      format: date-time
                      type: string
                  required:
                  - newScale
                  - previousScale
                  - reason
                  - success
                  - timestamp
                  type: object
                maxItems: 5
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
